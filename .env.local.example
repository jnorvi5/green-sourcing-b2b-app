# ============================================
# LOCAL DEVELOPMENT ENVIRONMENT VARIABLES
# ============================================
#
# Copy this file to .env.local and fill in your actual values
# cp .env.local.example .env.local
#
# ⚠️  DO NOT COMMIT .env.local - it should be in .gitignore
#
# ============================================

# ============================================
# NEXTAUTH v5 AUTHENTICATION (Local Development)
# ============================================

# Auth.js Configuration (v5 supports both NEXTAUTH_* and AUTH_* prefixes)
NEXTAUTH_URL=http://localhost:3000
# Alternative: AUTH_URL=http://localhost:3000

# Generate a random secret with: openssl rand -base64 32
NEXTAUTH_SECRET=GENERATE_A_LONG_RANDOM_STRING
# Alternative: AUTH_SECRET=GENERATE_A_LONG_RANDOM_STRING

# ============================================
# MICROSOFT ENTRA EXTERNAL ID (Local Development)
# ============================================

# NextAuth v5 expects these variable names:
AUTH_MICROSOFT_ENTRA_ID_ID=your_external_id_app_client_id
AUTH_MICROSOFT_ENTRA_ID_SECRET=your_external_id_client_secret

# External ID issuer format (example):
# https://<tenant_subdomain>.ciamlogin.com/<tenant_id>/v2.0
AUTH_MICROSOFT_ENTRA_ID_ISSUER=https://your-tenant-subdomain.ciamlogin.com/your-tenant-id/v2.0

# Optional legacy vars used by some services
AZURE_AD_CLIENT_ID=your_external_id_app_client_id
AZURE_AD_CLIENT_SECRET=your_external_id_client_secret
AZURE_AD_TENANT_ID=your-tenant-id

# ============================================
# DATABASE (Local Development)
# ============================================

# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/greenchainz_dev

# ============================================
# OPTIONAL: Additional Configuration
# ============================================

# Node environment
NODE_ENV=development

# Backend URL (if using separate backend)
BACKEND_URL=http://localhost:3001
